apiVersion: gobackup.io/v1alpha1
kind: Storage
metadata:
  name: sftp
  namespace: gobackup-e2e-test
spec:
  type: sftp
  config:
    sftp:
      host: sftp.gobackup-e2e-test
      port: 22
      username: root
      password: password
      path: /upload
      keep: 5
