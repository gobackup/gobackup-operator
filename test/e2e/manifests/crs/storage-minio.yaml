apiVersion: gobackup.io/v1
kind: Storage
metadata:
  name: minio
  namespace: gobackup-e2e-test
spec:
  type: minio
  config:
    bucket: test-bucket
    endpoint: http://minio.gobackup-e2e-test:9000
    region: us-east-1
    access_key_id: minioadmin
    secret_access_key: minioadmin
    force_path_style: true
