---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: databases.gobackup.io
spec:
  group: gobackup.io
  names:
    categories:
    - gobackup
    kind: Database
    listKind: DatabaseList
    plural: databases
    shortNames:
    - db
    singular: database
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Database is the Schema for the databases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              DatabaseSpec defines the desired state of Database.

              Config carries one nested sub-object per database type; exactly the
              sub-object matching spec.type must be set. The two CEL rules below enforce
              that invariant (the emitter flattens the chosen sub-object into gobackup.yml).
            properties:
              config:
                description: Config carries exactly one nested sub-object matching
                  spec.type.
                properties:
                  etcd:
                    description: 'Etcd configuration (spec.type: etcd).'
                    properties:
                      args:
                        description: Args are additional etcdctl arguments.
                        type: string
                      endpoint:
                        description: Endpoint is a single etcd endpoint.
                        type: string
                      endpoints:
                        description: Endpoints are the etcd endpoints.
                        items:
                          type: string
                        type: array
                    type: object
                  firebird:
                    description: 'Firebird configuration (spec.type: firebird).'
                    properties:
                      args:
                        description: Args are additional gbak arguments.
                        type: string
                      database:
                        description: Database is the database path/name.
                        type: string
                      host:
                        description: Host is the Firebird server hostname.
                        type: string
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        description: Port is the Firebird server port.
                        type: integer
                      role:
                        description: Role is the SQL role to assume.
                        type: string
                      username:
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  influxdb:
                    description: 'InfluxDB configuration (spec.type: influxdb).'
                    properties:
                      all_databases:
                        description: AllDatabases backs up all buckets.
                        type: boolean
                      bucket:
                        description: Bucket is the bucket name.
                        type: string
                      bucket_id:
                        description: BucketID is the bucket ID.
                        type: string
                      host:
                        description: Host is the InfluxDB server URL/host.
                        type: string
                      http_debug:
                        description: HTTPDebug enables HTTP debug logging.
                        type: boolean
                      org:
                        description: Org is the organization name.
                        type: string
                      org_id:
                        description: OrgID is the organization ID.
                        type: string
                      skip_verify:
                        description: SkipVerify skips TLS certificate verification.
                        type: boolean
                      token:
                        description: Token is the InfluxDB API token. Use token_ref
                          to reference a Secret instead.
                        type: string
                      token_ref:
                        description: TokenRef references a Secret containing the InfluxDB
                          API token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  mariadb:
                    description: 'MariaDB configuration (spec.type: mariadb).'
                    properties:
                      all_databases:
                        description: AllDatabases dumps all databases.
                        type: boolean
                      args:
                        description: Args are additional mariadb-dump arguments.
                        type: string
                      database:
                        description: Database is the database name.
                        type: string
                      host:
                        default: 127.0.0.1
                        description: Host is the MariaDB server hostname.
                        type: string
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 3306
                        description: Port is the MariaDB server port.
                        type: integer
                      socket:
                        description: Socket is the MariaDB server socket path.
                        type: string
                      username:
                        default: root
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  mongodb:
                    description: 'MongoDB configuration (spec.type: mongodb).'
                    properties:
                      all_databases:
                        description: AllDatabases dumps all databases.
                        type: boolean
                      args:
                        description: Args are additional mongodump arguments.
                        type: string
                      authdb:
                        description: AuthDB is the authentication database.
                        type: string
                      database:
                        description: Database is the database name.
                        type: string
                      exclude_tables:
                        description: ExcludeTables excludes these collections from
                          the dump.
                        items:
                          type: string
                        type: array
                      exclude_tables_prefix:
                        description: ExcludeTablesPrefix excludes collections matching
                          this prefix.
                        type: string
                      host:
                        default: 127.0.0.1
                        description: Host is the MongoDB server hostname.
                        type: string
                      oplog:
                        description: Oplog enables a point-in-time oplog backup.
                        type: boolean
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 27017
                        description: Port is the MongoDB server port.
                        type: integer
                      uri:
                        description: URI is the full MongoDB connection URI.
                        type: string
                      username:
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  mssql:
                    description: 'MSSQL configuration (spec.type: mssql).'
                    properties:
                      all_databases:
                        description: AllDatabases dumps all databases.
                        type: boolean
                      args:
                        description: Args are additional sqlcmd/bcp arguments.
                        type: string
                      database:
                        description: Database is the database name.
                        type: string
                      host:
                        default: 127.0.0.1
                        description: Host is the SQL Server hostname.
                        type: string
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 1433
                        description: Port is the SQL Server port.
                        type: integer
                      skip_databases:
                        description: SkipDatabases is a comma-separated list of databases
                          to skip.
                        type: string
                      trust_server_certificate:
                        description: TrustServerCertificate trusts the server TLS
                          certificate.
                        type: boolean
                      username:
                        default: sa
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  mysql:
                    description: 'MySQL configuration (spec.type: mysql).'
                    properties:
                      all_databases:
                        description: AllDatabases dumps all databases.
                        type: boolean
                      args:
                        description: Args are additional mysqldump arguments.
                        type: string
                      database:
                        description: Database is the database name.
                        type: string
                      exclude_tables:
                        description: ExcludeTables excludes these tables from the
                          dump.
                        items:
                          type: string
                        type: array
                      host:
                        default: 127.0.0.1
                        description: Host is the MySQL server hostname.
                        type: string
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 3306
                        description: Port is the MySQL server port.
                        type: integer
                      socket:
                        description: Socket is the MySQL server socket path.
                        type: string
                      tables:
                        description: Tables restricts the dump to these tables.
                        items:
                          type: string
                        type: array
                      username:
                        default: root
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  postgresql:
                    description: 'PostgreSQL configuration (spec.type: postgresql).'
                    properties:
                      all_databases:
                        description: AllDatabases dumps all databases (pg_dumpall).
                        type: boolean
                      args:
                        description: Args are additional pg_dump arguments.
                        type: string
                      compress:
                        description: Compress passes the pg_dump compression option.
                        type: string
                      database:
                        description: Database is the database name.
                        type: string
                      exclude_tables:
                        description: ExcludeTables excludes these tables from the
                          dump.
                        items:
                          type: string
                        type: array
                      host:
                        default: localhost
                        description: Host is the PostgreSQL server hostname.
                        type: string
                      password:
                        description: Password is the database password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          database password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 5432
                        description: Port is the PostgreSQL server port.
                        type: integer
                      socket:
                        description: Socket is the PostgreSQL server socket path.
                        type: string
                      tables:
                        description: Tables restricts the dump to these tables.
                        items:
                          type: string
                        type: array
                      username:
                        description: Username is the database username. Use username_ref
                          to reference a Secret instead.
                        type: string
                      username_ref:
                        description: UsernameRef references a Secret containing the
                          database username.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  redis:
                    description: 'Redis configuration (spec.type: redis).'
                    properties:
                      args:
                        description: Args are additional redis-cli arguments.
                        type: string
                      host:
                        default: 127.0.0.1
                        description: Host is the Redis server hostname.
                        type: string
                      invoke_save:
                        default: true
                        description: InvokeSave runs SAVE before copying the dump
                          file.
                        type: boolean
                      mode:
                        default: copy
                        description: Mode is the Redis dump mode.
                        enum:
                        - copy
                        - sync
                        type: string
                      password:
                        description: Password is the Redis password. Use password_ref
                          to reference a Secret instead.
                        type: string
                      password_ref:
                        description: PasswordRef references a Secret containing the
                          Redis password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 6379
                        description: Port is the Redis server port.
                        type: integer
                      rdb_path:
                        description: RdbPath is the path to dump.rdb.
                        type: string
                      socket:
                        description: Socket is the Redis server socket path.
                        type: string
                    type: object
                  sqlite:
                    description: 'SQLite configuration (spec.type: sqlite).'
                    properties:
                      path:
                        description: Path is the path to the SQLite database file.
                        type: string
                    type: object
                type: object
              type:
                description: Type is the database backend type.
                enum:
                - postgresql
                - mysql
                - mariadb
                - mongodb
                - redis
                - mssql
                - influxdb
                - etcd
                - firebird
                - sqlite
                type: string
            required:
            - config
            - type
            type: object
            x-kubernetes-validations:
            - message: exactly one config sub-object must be set
              rule: (has(self.config.postgresql)?1:0)+(has(self.config.mysql)?1:0)+(has(self.config.mariadb)?1:0)+(has(self.config.mongodb)?1:0)+(has(self.config.redis)?1:0)+(has(self.config.mssql)?1:0)+(has(self.config.influxdb)?1:0)+(has(self.config.etcd)?1:0)+(has(self.config.firebird)?1:0)+(has(self.config.sqlite)?1:0)
                == 1
            - message: the config sub-object must match spec.type
              rule: (self.type == 'postgresql' && has(self.config.postgresql)) ||
                (self.type == 'mysql' && has(self.config.mysql)) || (self.type ==
                'mariadb' && has(self.config.mariadb)) || (self.type == 'mongodb'
                && has(self.config.mongodb)) || (self.type == 'redis' && has(self.config.redis))
                || (self.type == 'mssql' && has(self.config.mssql)) || (self.type
                == 'influxdb' && has(self.config.influxdb)) || (self.type == 'etcd'
                && has(self.config.etcd)) || (self.type == 'firebird' && has(self.config.firebird))
                || (self.type == 'sqlite' && has(self.config.sqlite))
          status:
            description: DatabaseStatus defines the observed state of Database
            properties:
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent Database spec generation observed by
                  the controller. There is no dedicated Database controller yet, so this is
                  currently unpopulated; it exists to make the status subresource honest.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
