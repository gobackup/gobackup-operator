apiVersion: gobackup.io/v1alpha1
kind: Database
metadata:
  name: example-mariadb-direct
  namespace: default
spec:
  type: mariadb
  config:
    mariadb:
      host: mariadb.example.svc.cluster.local
      port: 3306
      database: exampledb
      username: root
      password: examplepassword
