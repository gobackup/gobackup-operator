apiVersion: gobackup.io/v1alpha1
kind: Database
metadata:
  name: example-redis-direct
  namespace: default
spec:
  type: redis
  config:
    redis:
      host: redis.example.svc.cluster.local
      port: 6379
      password: examplepassword
      mode: sync
